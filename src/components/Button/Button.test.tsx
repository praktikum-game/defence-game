// import React from 'react';
// import renderer from 'react-test-renderer';
// import { Button } from './Button';

describe('test Button component', () => {
  it('constructor and props', () => {
    expect.assertions(1);
    expect(2).toBe(2);
  });
});
