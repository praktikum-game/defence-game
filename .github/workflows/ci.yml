name: APP-CI
on:
  push:
    branches:
      - main
      - sprint_9/ci_gh_actions
  pull_request:
    branches:
      - main
build:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v2
    - name: Run tests
    run: |
      npm ci,
      npm run test.